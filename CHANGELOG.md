# 更新说明

本文件的内容会显示给采购同事——他们在更新器界面上看到的「这次更新改了什么」
就来自这里。发布时由 scripts/publish-updater.sh 按版本号取出对应段落，
写进 latest.json 的 notes 字段。

格式要求（解析依赖，勿改）：
- 每个版本一个 `## 版本号` 二级标题，版本号不带 v 前缀
- 标题下面的正文即为该版本的说明，直到下一个 `##` 为止

写给非技术同事看，说清「有什么变化」即可，不要写实现细节。

## 0.4.5

- 新增「自动检查更新」开关（默认关闭），托盘右键菜单可开启，开启后每小时自动检查一次
- 未开启自动检查时，界面顶部会有提示，仍可随时手动点「立即检查更新」

## 0.2.6

- 新增「机器状态」页，可查看当前电脑的内存、CPU占用、磁盘剩余空间和系统版本，方便判断是否卡顿
- 顶部常驻显示简化版电脑状态（CPU占用+可用内存），负载过高时变红提醒
- 修复插件在 1688 等网页上产生的日志收不到的问题，现在能正常记录了
- 修复插件日志显示的时间比实际早 8 小时，现在统一显示北京时间

## 0.2.5

- 修复自动更新安装失败：安装包改为当前用户模式，不再需要管理员权限

## 0.2.4

- 更新失败时直接显示失败原因，不用再去翻日志
- 记录下载与安装进度，便于排查升级问题

## 0.2.3

- 修复更新失败时一直显示「正在下载」不消失的问题

## 0.2.2

- 新增「立即检查更新」：托盘右键菜单和界面上都能点，不用等自动检查
- 减少无用的错误日志

## 0.2.1

- 界面上直接显示当前版本号，不用再翻日志确认
- 更新提示会说明这次更新改了什么

## 0.2.0

- 更新器支持自动更新：以后新版本会自动下载安装，不需要手动替换程序
- 修复日志中来源标注不准确的问题

## 0.1.0

- 首个具备自动更新能力的版本
